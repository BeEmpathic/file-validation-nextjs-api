# This is file validation with nextjs using API app routes

I used app routes cause I think they are better for file validation than server actions, but there is a chance that if you just use nginx as reverse proxy and use lib / package called formaidable you will be good to go cause this is what I'm about to do with API app routes

There is a chance that it might not be good 

## How to get it to work?
If you just clone it should work

